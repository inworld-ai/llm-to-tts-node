{
  "name": "llm-to-tts-node",
  "version": "1.0.0",
  "description": "A production-ready pipeline that processes user input through an LLM and converts the response to speech using TTS",
  "type": "module",
  "main": "graph.ts",
  "scripts": {
    "start": "inworld-runtime serve graph.ts",
    "run": "inworld-runtime run graph.ts",
    "deploy": "inworld-runtime deploy graph.ts"
  },
  "keywords": [
    "llm",
    "tts",
    "ai",
    "graph",
    "runtime",
    "inworld"
  ],
  "author": "Inworld AI",
  "license": "MIT",
  "dependencies": {
    "@inworld/runtime": "^1.0.7"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
